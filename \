{ ... }:

let
  theme = import ../style/themes/catpuccin-clout-theme.nix;
in
{
  programs.alacritty = {
    enable = true;

    settings = {

      window = {
        opacity = 0.9;
      };

      colors = with theme.colors; {
        primary = {
          background = base;

	};
      };
    };
  };
}
